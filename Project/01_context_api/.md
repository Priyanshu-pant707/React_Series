- solves the probe drilling problem
- example:
 - state value =1;
 -  state value is in compnent i A;
 - A->B->C->D  
 - now yo have to access the state value in D.
 - using props ->cause props drilling.
- the context should be wrapped with the context provider 
